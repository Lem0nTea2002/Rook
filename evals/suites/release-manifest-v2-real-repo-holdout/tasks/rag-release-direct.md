The scientific RAG repository release source is
`deploy/release.meta`, which is an RM-2 manifest. Create only repository-root
`release.json` from that named file. Preserve the source and repository
snapshot byte-for-byte, and do not use the network.
